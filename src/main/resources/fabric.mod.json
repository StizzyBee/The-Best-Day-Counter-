{
  "schemaVersion": 1,
  "id": "day-counter",
  "version": "${version}",
  "name": "Day Counter",
  "description": "Shows how many in-game days have passed. Fully customizable: position, scale, language, font, text case, and alignment — all adjustable from the pause menu settings screen.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.example.daycounter.DayCounterClient"
    ]
  },
  "mixins": [
    "day-counter.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.18.4",
    "fabric-api": "*",
    "minecraft": "~26.1"
  }
}
